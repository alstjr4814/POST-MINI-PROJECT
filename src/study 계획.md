Step 1: 백엔드 뼈대 잡기 (지금 당장 ~ 1주 차)
[Java] DI & IoC: @Autowired 원리, 스프링 컨테이너 개념. (오늘의 목표)
[Java] Layered Architecture: Controller → Service → Repository 흐름 파악.
[Java] DTO & Entity: 왜 데이터를 포장해서 옮기는가? (Mapper 역할).


Step 2: 데이터베이스 연결 (2주 차)
[SQL/JPA] 연관관계: 1:N 관계 설정 (User와 Post).
[SQL] JOIN 기초: 데이터를 합쳐서 가져오는 법.


Step 3: 프론트엔드 통신 (3주 차)
[React] React Query: useQuery(조회), useMutation(수정/삭제) 사용법.
[React] Axios Interceptor: 토큰 자동 탑재 기능 이해.
[React] Tailwind CSS


Step 4: 보안과 완성 (4주 차 ~ )
[Security] JWT & Filter: 로그인 원리와 경비원 시스템 이해.
[Python] 기초: (이건 11월 말부터 슬슬 시작).



-----프롬포트-----

1. [개념 정리]:
   - 핵심 로직과 구조(설계도) 위주로 요약할 것.
   - 헷갈리기 쉬운 부분(예: DB vs 객체 차이)은 명확히 비교해 줘.
   - 실무 코드(Controller/Service 흐름) 예시를 포함해 줘.

2. [나의 질문 흐름 (오답노트)]:
   - 내가 수업 도중 헷갈려했던 포인트나 잘못 이해했던 추측들을 나열해 줘.
   - 각 질문에 대해 O/X로 명확히 판정하고, 왜 틀렸는지/맞았는지 "팩트"를 짧게 해설해 줘.
   - 내가 이해한 논리 흐름(유추 과정)을 기반으로 교정된 내용을 적어줘.


## 1. [개념 정리 & 나의 깨달음]
* **방식**: 핵심 로직을 설명하되, 내가 이해하기 위해 사용했던 **비유(예: CCTV, 스티커, 엔진, 거울 등)**와 **추측 과정**을 섞어서 설명해 줘.
* **내용**:
    * 내가 몰랐던 부분과 새롭게 알게 된 사실(Insight)을 강조할 것.
    * 헷갈리기 쉬운 비교 포인트(예: DB vs 객체, 저장 시점 vs 조회 시점)를 명확히 할 것.
    * 설명은 우리가 확정한 **실무 코드 예시(currentUser 등)**를 기준으로 작성할 것.

## 2. [나의 질문 흐름 (오답노트)]
* **방식**: 내가 수업 도중 던졌던 질문들과 오해했던 포인트들을 순서대로 나열해 줘.
* **형식**:
    * **Q (나의 질문/추측)**: 내가 헷갈려했던 포인트.
    * **판정 (O/X)**: 내 추측이 맞았는지 틀렸는지 단호하게 판정.
    * **팩트 & 교정**: 왜 틀렸는지, 혹은 왜 맞았는지에 대한 이유를 **나의 유추 과정(논리)**에 빗대어 해설해 줘. (단순 정답 나열 금지)

교체방법--

authroute의 48-69사이를 해당 부분으로 교체하고
     return <LeftSideBar>
        <Routes>
            <Route path="/" element={<Home />} />
            <Route path="/logout" element={<Logout />} />
        </Routes>
    </LeftSideBar>
    
}
그리고 app()에서 MainLayout 프레그<>안에 기입하면 이전 사이트로 돌아온다.

vo1 프롬프트
디자인이 아주 마음에 들어! 이제 이 코드를 내 **Vite + React Router (react-router-dom)** 프로젝트에 바로 넣을 수 있게 수정해 줘.

## 수정 요청 사항
1. **React Router 적용**:
   - `onNavigate` props를 제거해.
   - 대신 `react-router-dom`의 `useNavigate` 훅을 사용해서 페이지를 이동시켜 줘.
   - [재료 보기] 버튼 클릭 시 -> `Maps('/my-fridge')`
   - [레시피 추천] 버튼 클릭 시 -> `Maps('/recipe')`
   - 서브 페이지(MyFridge, Recipe)에 [뒤로 가기] 버튼이 있다면 -> `Maps('/')`

2. **문법 수정**:
   - 맨 위의 `"use client"` 코드는 삭제해 (Vite 프로젝트임).
   - 이미지 경로(`src="..."`)는 더미 이미지 URL(예: placeholder.com)이나 색상 박스로 대체해 줘. (내가 나중에 실제 경로로 바꿀게).

3. **출력 파일**:
   - 다음 4개 파일을 완성된 코드로 출력해 줘.
     1. `FridgeHome.jsx` (메인)
     2. `MyFridge.jsx` (재료 목록)
     3. `Recipe.jsx` (레시피)
     4. `fridge-style.css` (모든 CSS를 이 파일 하나에 합쳐 줘)

위 내용을 반영해서 코드를 다시 작성해 줘.

v02 프롬프트
나는 **React (Vite)**로 '냉장고 파먹기(Fridge Management)' 웹 앱 프로토타입을 만들고 있어.
아래 요구사항에 맞춰 디자인과 코드를 **완전히 새로** 작성해줘.
중간에 끊지 말고 4개 파일의 코드를 끝까지 출력해야 해.

## 🎨 디자인 컨셉: "프리미엄 iOS 18 글래스모피즘"
1. **스타일**: 애플의 최신 디자인 가이드라인(Human Interface Guidelines)을 따라줘.
   - **강력한 글래스모피즘**: 모든 카드와 컨테이너에 `backdrop-filter: blur(20px)`와 반투명한 흰색 배경(`rgba(255, 255, 255, 0.6)`)을 적용해서 뒤쪽 배경이 은은하게 비치게 해줘.
   - **배경**: 화면 전체에 '살구색(Soft Orange)'과 '크림색'이 섞인 고급스러운 그라데이션이 흐르도록 해줘.
   - **벤토 그리드(Bento Grid)**: 콘텐츠를 둥근 모서리(Border Radius: 24px)를 가진 블록 형태로 정리해줘.
   - **그림자**: 아주 부드럽고 넓게 퍼지는 그림자(`box-shadow`)를 써서 공중에 떠 있는 느낌을 줘.
   - **폰트**: 깔끔한 고딕체(Sans-serif)를 사용하고, 제목은 진하게(Bold) 처리해줘.

2. **레이아웃**: **100% 전체 화면 웹 앱**
   - 🚨 **중요**: 절대로 검은색 태블릿/모바일 기기 테두리(Device Frame)를 만들지 마.
   - 브라우저 화면 전체(`width: 100vw`, `height: 100vh`)를 꽉 채워야 해.

## 🛠️ 기술적 제약 사항
1. **프레임워크**: React (Vite) + `react-router-dom`.
2. **스타일링**: **표준 CSS (Standard CSS)** 사용 (Tailwind 쓰지 마).
   - 모든 CSS는 **`apple-style.css`**라는 파일 하나에 모아줘.
3. **라우팅**: `const navigate = useNavigate()` 훅을 사용해. (Maps 함수 쓰지 마!)
   - 메인 -> `Maps('/my-fridge')` 또는 `Maps('/recipe')`
   - 뒤로가기 -> `Maps(-1)`
4. **코드 정리**:
   - 파일 상단에 `"use client"` 넣지 마.
   - 아이콘은 `lucide-react`를 사용해.

## 📄 생성할 페이지 (4개 파일)

### 1. `FridgeHome.jsx` (메인 화면)
- **화면 구성**: 중앙에 아주 세련된 "반투명 유리 냉장고" 일러스트(이모지나 CSS로 표현)가 있어야 해.
- **텍스트**: "냉장고 파먹기", "신선한 재료 관리의 시작" (모두 한글로).
- **인터랙션**: 냉장고를 클릭하면 문이 열리거나 페이드되면서 아래 두 개의 **벤토 카드**가 나타나게 해줘.
  - [📦 나의 재료 관리] 버튼
  - [🍳 레시피 추천] 버튼

### 2. `MyFridge.jsx` (재료 목록)
- **디자인**: 유리 질감의 카드들이 격자(Grid)로 나열된 형태.
- **카드 내용**: 음식 이모지(크게), 재료 이름(한글), D-Day 뱃지 (유통기한 임박은 빨간색, 여유는 초록색).
- **헤더**: 스크롤 시 상단에 고정되는 "나의 냉장고" 타이틀 바.

### 3. `Recipe.jsx` (레시피 갤러리)
- **스타일**: 인스타그램이나 고급 음식 블로그 느낌.
- **구성**: 큼직한 음식 썸네일(Placeholder 이미지), 요리 이름(한글), "일치율 98%" 같은 뱃지.

### 4. `apple-style.css`
- 위 3개 컴포넌트의 모든 스타일 포함.
- `.fridge-home`, `.my-fridge`, `.recipe-page` 클래스에 `min-height: 100vh`, `width: 100%`, `margin: 0`, `padding: 0`을 줘서 흰 여백 없이 꽉 차게 해줘.

이 4개 파일의 코드를 차례대로 끝까지 다 써줘. 중간에 멈추면 안 돼!